// CustomLoader.tsx
import React from 'react';
import styles from '@/styles/components/atoms/CustomLoader.module.scss';

const CustomLoader: React.FC = () => {
  return <div className={styles.loader}></div>;
};

export default CustomLoader;
