// TEMPORARY preview route for pixel-perfect verification of SocialOrbitSection.
// Safe to delete — not linked anywhere in the app.
import SocialOrbitSection from "@/components/organisms/SocialOrbitSection";

export default function OrbitPreviewPage() {
  return (
    <main style={{ background: "#ffffff", minHeight: "100vh" }}>
      <SocialOrbitSection />
    </main>
  );
}
