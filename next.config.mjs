/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.postimg.cc','img.icons8.com', 'api.microlink.io', 'via.placeholder.com'], 
    },
  };
  
  export default nextConfig;
  
